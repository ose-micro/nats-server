port: 4222

# Monitoring endpoint
http: 8222

# JetStream configuration
jetstream {
  store_dir: /data/jetstream
  max_mem: 1Gb
  max_file: 10Gb
}

# Authorization
authorization {
  users = [
    { user: ${NATS_USER}, password: ${NATS_PASS} }
  ]
}
